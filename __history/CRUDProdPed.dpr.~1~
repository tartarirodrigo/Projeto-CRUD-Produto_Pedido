program CRUDProdPed;

uses
  Vcl.Forms,
  pTelaLogin in 'pTelaLogin.pas' {Form1},
  pCadEmpresa in 'pCadEmpresa.pas' {Form2};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
